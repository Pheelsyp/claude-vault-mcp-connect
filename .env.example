# Copy this file to .env and fill in your values.
# Then export them before launching VS Code:
#   export $(cat .env | xargs) && code .

VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=your-vault-token-here
